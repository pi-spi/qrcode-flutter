name: bceao_pispi_qrcode
description: "Flutter SDK pour générer et décoder des QR Codes PI-SPI conformes EMV."
version: 1.0.6
homepage: "https://github.com/pi-spi/qrcode-flutter.git"
repository: "https://github.com/pi-spi/qrcode-flutter.git"

environment:
  sdk: ^3.10.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ^1.0.0

  #
  qr: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  # plugin:
  #   platforms:
  #     android:
  #       package: bcecao.pispi.qrcode.bceao_pispi_qrcode
  #       pluginClass: BceaoPispiQrcodePlugin
  #     ios:
  #       pluginClass: BceaoPispiQrcodePlugin
  #     web:
  #       pluginClass: BceaoPispiQrcodeWeb
  #       fileName: pispi_qr_web.dart

  # To add assets to your plugin package, add an assets section, like this:
  assets:
    - assets/images/
